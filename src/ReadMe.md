# 📁 src/README.md

```md
# src

This folder contains all **source code** for the COG (CSV–OWL Bridge) project.

## Subfolders

- `csv_to_owl/`  
  Implements CSV/XLSX → OWL conversion

- `owl_to_csv/`  
  Implements OWL → CSV/XLSX conversion

## Main Script

- `start.py`  
  Entry point for running conversions

## Example Commands

OWL → CSV/XLSX

```bash
python start.py owl_to_csv "path/to/input.owl"
